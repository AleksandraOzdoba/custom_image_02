os: linux
dist: focal
language: generic
vm:
  create:
    name: other_user01
before_install:
  - sudo apt-get -y install libxml2-dev
addons:
  apt:
    update: true
script:
  - echo $TRAVIS_COMMIT_MESSAGE
  - sleep 2
  - echo "OK"
